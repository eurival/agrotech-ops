apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaConnect
metadata:
  name: agro-connect
  namespace: agrotech
  annotations:
    strimzi.io/use-connector-resources: "true"
spec:
  version: 3.7.0
  replicas: 1
  bootstrapServers: agro-cluster-kafka-bootstrap:9092
  
  # 1. Imagem Oficial Strimzi (Garantia de existir)
  image: quay.io/strimzi/kafka:0.40.0-kafka-3.7.0
  
  # 2. A Mágica: Injetar o plugin na inicialização
  template:
    pod:
      volumes:
      - name: connector-plugins
        emptyDir: {}
      initContainers:
      - name: download-plugins
        image: busybox:1.36
        command: ["sh", "-c"]
        args:
          - |
            echo "Baixando plugin MQTT..."
            mkdir -p /opt/kafka/plugins/mqtt
            # Baixa o ZIP oficial da Streamthoughts (Versão compatível)
            wget -q -O /tmp/plugin.zip https://github.com/streamthoughts/kafka-connect-mqtt/releases/download/v2.3.0/streamthoughts-kafka-connect-mqtt-2.3.0.zip
            # Descompacta
            unzip -q /tmp/plugin.zip -d /tmp/
            # Move o JAR para a pasta de plugins
            mv /tmp/streamthoughts-kafka-connect-mqtt-2.3.0/* /opt/kafka/plugins/mqtt/
            echo "Plugin instalado com sucesso!"
        volumeMounts:
        - name: connector-plugins
          mountPath: /opt/kafka/plugins
      containers:
      - name: kafka-connect
        volumeMounts:
        - name: connector-plugins
          mountPath: /opt/kafka/plugins

  config:
    group.id: connect-cluster
    offset.storage.topic: connect-cluster-offsets
    config.storage.topic: connect-cluster-configs
    status.storage.topic: connect-cluster-status
    key.converter: org.apache.kafka.connect.storage.StringConverter
    value.converter: org.apache.kafka.connect.json.JsonConverter
    value.converter.schemas.enable: false
    # Avisa pro Connect onde buscar os plugins que o InitContainer baixou
    plugin.path: /opt/kafka/plugins