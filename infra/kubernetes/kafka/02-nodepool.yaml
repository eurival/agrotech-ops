apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaNodePool
metadata:
  name: pool-misto
  namespace: agrotech
  labels:
    strimzi.io/cluster: agro-cluster
spec:
  replicas: 1 # Começando com 1 nó
  roles:
    - controller
    - broker
  storage:
    type: jbod
    volumes:
      - id: 0
        type: persistent-claim
        size: 10Gi
        class: nfs-vm-storage-retain-v3
        deleteClaim: false